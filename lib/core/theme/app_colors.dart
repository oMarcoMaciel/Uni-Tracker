import 'package:flutter/material.dart';

class AppColors {
  // Fundo principal (Dark Green muito escuro)
  static const Color background = Color(0xFF0A120E); 
  
  // Fundo dos Cards (Um pouco mais claro)
  static const Color surface = Color(0xFF16261E); 
  
  // A cor de destaque (Neon Green)
  static const Color primary = Color(0xFF00E676); 
  
  // Texto
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFAAAAAA);
  
  // Cores semânticas (Notas, Avisos)
  static const Color accentBlue = Color(0xFF2979FF);
  static const Color accentPurple = Color(0xFFD500F9);
  static const Color accentOrange = Color(0xFFFF9100);
}